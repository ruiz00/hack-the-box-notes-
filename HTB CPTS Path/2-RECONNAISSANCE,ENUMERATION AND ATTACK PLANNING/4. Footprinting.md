 Enumeration stands for the information gathering using active(scans) and passive(use of third-party providers)methods. Information can be gathered from domains , IP addresses, accessible services and many other sources.
 **Our goal is not to get at the system but to find all the possible ways to get there.**
 
- What can we see?
- What reasons can we have for seeing it?
- What image does what we see create for us?
- What do we gain from it?
- How can we use it?
- What can we not see?
- What reasons can there be that we do not see?
- What image results for us from what we do not see?

| no  | Principle                                                              |
| --- | ---------------------------------------------------------------------- |
| 1.  | There is more than meets the eye. Consider all points of view.         |
| 2.  | Distinguish between what we see and what we do not see.                |
| 3.  | There are always ways to gain more information. Understand the target. |
## DOMAIN INFORMATION
	the first presence of a company online is it SSL certificate and for many companies it can be helpful because it help to determine subdomains using the same ssl certificate 
	Another source for ssl certificate is crt.sh (certificate transparencey)
	we can obtain ssl inforamtion using the command below and output it in JSON format

```
	curl -s https://crt.sh/\?q\=inlanefreight.com\&output\=json | jq .
```
Filter results to unique subdomains
```
	curl -s https://crt.sh/\?q\=inlanefreight.com\&output\=json | jq . | grep name | cut -d":" -f2 | grep -v "CN=" | cut -d'"' -f2 | awk '{gsub(/\\n/,"\n");}1;' | sort -u
```
 Company hosted servers
 ```
 for i in $(cat subdomainlist);
 do host $i | grep "has address" | grep inlanefreight.com | cut -d" " -f1,4;
 done
 ```
Another handy tool for OSINT is Shodan which can be used to find devices and systems permanently connected to the internet.
```
for i in $(cat subdomainlist);
do host &i | grep " has address" | grep inlanefreight.com | cut -d" " -f4 >> ip_addresses.txt;done 
for i in $(cat ip-addresses.txt);
do shodan host $i; done
```

Now we can perform a list of DNS revserse can to list all DNS records
```
dig any inlanefrieght.com
```

- A record recognises the IP addresses that point to a particular (sub)domain 
- MX record show information about mail servers
- NS record are used to show which server resolve FQDN to IP addresses
- TXT often contain keys for different third party providers and other security aspects of DNS such as SPF,DMARC and DKIM which are responsible for verifying and confirming the origin of the emails sent.
## CLOUD RESOURCES
The use of the cloud give another surface where vulnerabilities can be found if not well configured by system administrator and hence also needs our attention.
One of the easiest and most used eay to find cloud storages is the use of Google Dorks ***inurl***
and ***intext*** to narrow our search to specfic terms
Target website source code can also help us find cloud storage a company might be using
Domain.glass and GrayHatWarfare can also tell us a lot about the company's infrastructure.

## FTP
 ```
 ls -R : recursive listing
 get Important\ Notes.txt : Downloading a file
 wget -m --no-passive ftp://anonymous:anonymous@10.129.14.136 : all files download
 nb: done directly from our attacking machine shell
 put upload.txt : upload file to remote server
 quit : ecit ftp interactive shell
 connect : connect to an ftp server
 ```
Footprinting the ftp service
```
find / -type f -name ftp* 2>/dev/null | grep scripts : find all nse script for ftp
sudo nmap -sVC -A 10.129.14.136 : aggressive with default nse script for nmap scan 

```
## SMB
- NetBIOS - Networking Basic Input Output System
- NetBIOS Name server (NBNS)
- Windows Internet Name Service(WINS)
- Remote Procdure Call (RPC)

```
smbclient -L -N //10.129.14.136 : connect to a share n for anon login and l for share listing
smbclient //10.129.14.169/notes : notes a share on the server
get : download a file from the server 
```
Footprinting the smb service
```
sudo nmap 10.129.14.128 -sV -sC -p139,145
rpcclient -U "" 10.129.14.128 : it perfoms MS-RPC functions 
rpcclient $> enumdomusers : use to enumerate users on ythe domain
			enumdomains, queryuser <RID>, netshareenumall (enum shares)
for i in $(seq 500 1100);do rpcclient -N -U "" 10.129.14.128 -c "queryuser 0x$(printf '%x\n' $i)" | grep "User Name\|user_rid\|group_rid" && echo "";done
used to brute force user RIDs
samrdump.py 10.129.14.128 : from impacket used to all brute force user RID
smbmap -H 10.129.14.128 
crackmapexec smb 10.129.14.128 --shares -u '' -p ''
./enum4linux-ng.py 10.129.14.128 -A
```
The information we have already obtained with `rpcclient` can also be obtained using other tools. For example, the [SMBMap](https://github.com/ShawnDEvans/smbmap) and [CrackMapExec](https://github.com/byt3bl33d3r/CrackMapExec) tools are also widely used and helpful for the enumeration of SMB services.
Another tool worth mentioning is the so-called [enum4linux-ng](https://github.com/cddmp/enum4linux-ng), which is based on an older tool, enum4linux.
## NFS (Network File System)

```
sudo nmap 10.129.14.128 -p111,2049 -sV --script-nfs*
showmount -e 10.129.14.128 : show shares
sudo mount -t nfs <address>:/ ./targer-NFS/ -o nolock
sudo unmount ./target-nfs
```
## DNS(Domain Name System)
|**DNS Record**|**Description**|
|---|---|
|`A`|Returns an IPv4 address of the requested domain as a result.|
|`AAAA`|Returns an IPv6 address of the requested domain.|
|`MX`|Returns the responsible mail servers as a result.|
|`NS`|Returns the DNS servers (nameservers) of the domain.|
|`TXT`|This record can contain various information. The all-rounder can be used, e.g., to validate the Google Search Console or validate SSL certificates. In addition, SPF and DMARC entries are set to validate mail traffic and protect it from spam.|
|`CNAME`|This record serves as an alias for another domain name. If you want the domain [www.hackthebox.eu](http://www.hackthebox.eu/) to point to the same IP as hackthebox.eu, you would create an A record for hackthebox.eu and a CNAME record for [www.hackthebox.eu](http://www.hackthebox.eu/).|
|`PTR`|The PTR record works the other way around (reverse lookup). It converts IP addresses into valid domain names.|
|`SOA`|Provides information about the corresponding DNS zone and email address of the administrative contact.|
```
dig <dns record> <address>
dig soa www.inlanefreight.com
```
Footprinting the service
```
dig ns inlanefreight.htb @10.129.14.128 : ns query
dig CH TXT version.bind 10.129.120.85 : version query
dig any inlanefreight.htb @10.129.14.128 : any
dig axfr inlanefreight.htb @10.129.14.128 : AXFR Zone transfer
dig axfr internal.inlanefreight.htb @ 10.129.14.128 : AXFR Zone transfer-internal
dnsenum --dnsserver 10.129.14.128 --enum -p 0 -s 0 -o subdomains.txt -f /opt/useful/seclists/Discovery/DNS/subdomains-top1million-110000.txt inlanefreight.htb : 

```
Many different tools can be used for this, and most of them work in the same way. One of these tools is, for example [DNSenum](https://github.com/fwaeytens/dnsenum).

## SMTP (Simple Mail Transfer Protocol)
```
telnet 10.129.14.128 25 
VRFY root : to connect to the smtp server
```
Footprinting the service
```
sudo nmap 10.129.14.128 -sV -p25  --script smtp-open-relay -v
```
## IMAP/POP3(Internet Message Access Protocol/Post Office Protocol)
IMAP Commands

| **Command**                     | **Description**                                                                                               |
| ------------------------------- | ------------------------------------------------------------------------------------------------------------- |
| `1 LOGIN username password`     | User's login.                                                                                                 |
| `1 LIST "" *`                   | Lists all directories.                                                                                        |
| `1 CREATE "INBOX"`              | Creates a mailbox with a specified name.                                                                      |
| `1 DELETE "INBOX"`              | Deletes a mailbox.                                                                                            |
| `1 RENAME "ToRead" "Important"` | Renames a mailbox.                                                                                            |
| `1 LSUB "" *`                   | Returns a subset of names from the set of names that the User has declared as being `active` or `subscribed`. |
| `1 SELECT INBOX`                | Selects a mailbox so that messages in the mailbox can be accessed.                                            |
| `1 UNSELECT INBOX`              | Exits the selected mailbox.                                                                                   |
| `1 FETCH <ID> all`              | Retrieves data associated with a message in the mailbox.                                                      |
| `1 CLOSE`                       | Removes all messages with the `Deleted` flag set.                                                             |
| `1 LOGOUT`                      | Closes the connection with the IMAP server.                                                                   |
POP3 Commands

|**Command**|**Description**|
|---|---|
|`USER username`|Identifies the user.|
|`PASS password`|Authentication of the user using its password.|
|`STAT`|Requests the number of saved emails from the server.|
|`LIST`|Requests from the server the number and size of all emails.|
|`RETR id`|Requests the server to deliver the requested email by ID.|
|`DELE id`|Requests the server to delete the requested email by ID.|
|`CAPA`|Requests the server to display the server capabilities.|
|`RSET`|Requests the server to reset the transmitted information.|
|`QUIT`|Closes the connection with the POP3 server.|
Footprinting the service
```
sudo nmap 10.129.14.128 -sV -p110,143,993,995 -sC
curl -k 'imaps://10.129.14.128' --user user:p4ssw0rd : login 
openssl s_client -connect 10.129.14.128:pop3s : encrypted interaction pop3
openssl s_client -connect 10.129.14.128:imaps :

```

## SNMP(Simple Network Management Protocol)
For footprinting SNMP, we can use tools like `snmpwalk`, `onesixtyone`, and `braa`. `Snmpwalk` is used to query the OIDs with their information. `Onesixtyone` can be used to brute-force the names of the community strings since they can be named arbitrarily by the administraton.
```
snmwalk -v2c -c poblic 10.129.14.128
onesixtyone -c /opt/useful/seclists/Discovery/SNMP/snmp.txt 10.129.14.128
braa <community string>@<IP>:.1.3.6.* : ex braa public@10.129.14.128:.1.3.6.*
```
## MySQL
```
sudo nmap 10.129.14.128 -sV -sC -p3306 --script mysql*
mysql -u root -h 10.129.14.132 : interactiong with server
MySQL [(none)]> show databases;
				select version();
				use mysql;
				show tables;
				
```
## MSSQL (Microsoft SQL)
```
sudo nmap --script ms-sql-info,ms-sql-empty-password,ms-sql-xp-cmdshell,ms-sql-config,ms-sql-ntlm-info,ms-sql-tables,ms-sql-hasdbaccess,ms-sql-dac,ms-sql-dump-hashes --script-args mssql.instance-port=1433,mssql.username=sa,mssql.password=,mssql.instance-name=MSSQLSERVER -sV -p 1433 10.129.201.248 : nmap mssql script scan
python3 mssqlclient.py Administrator@10.129.201.248 -windows-auth 
```

## Linux Remote Management Protocols
### SSH
```
git clone https://github.com/jtesta/ssh-audit.git && cd ssh-audit
./ssh-audit .py 10.129.14.128
```
### Rsync
```
sudo nmap -sV -p 873 127.0.0.1
nc -nv 127.0.0.1 873 : probing accessible shares
rsync -av --list-only rsync://127.0.0.1/dev : enum shares
 
```
### R-Services
```
sudo nmap -sV -p 512,513,514 10.0.17.2
sudo nmap -sV -p 512,513,514 10.0.17.2 : login using rlogin
rwho : listing auth user using rwho
rusers -al 10.0.17.5 : 
```

## Windows Remote Management Protocols
### RDP
```
nmap -sV -sC 10.129.201.248 -p3389 --script rdp*
nmap -sV -sC 10.129.201.248 -p3389 --packet-trace --disable-arp-ping -n :
sudo cpan , install Encoding::BER
git clone https://github.com/CiscoCXSecurity/rdp-sec-check.git && cd rdp-sec-check
./rdp-sec-check.pl 10.129.201.248
xfreerpd /u:cry0l1t3 /p:"P455w0rd!" /v:10.129.201.248 : login 

```
### WinRm
```
nmap -sV -sC 10.129.201.248 -p5985,5986 --disable-arp-ping -n
evil-winrm -i 10.129.201.248 -u Cry0l1t3 -p P455w0rD! : login

```

### WMIexec
```
/usr/share/doc/python3-impacket/examples/wmiexec.py Cry0l1t3:"P455w0rD!"@10.129.201.248 "hostname"
```