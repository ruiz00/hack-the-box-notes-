This process involves systematically and meticulously collecting information about a target website or web application. the primary goals of web recon include:
- Identifying assets:
- Discovring hidden information
- Analysing the attack surface
- Gathering intelligence
Types of reconnaissance:
- Active recon: Port scanning(Nmap,Masscan,Unicornscan), vulnerability scanning (Nessus, OpenVAS, Nikto), Network Mapper(Traceroute,nmap),Banner grabbing(Netcat,curl),Os fingerprinting,Service Enumeration , web Spdering (Burp Suite Spider, OWASP ZAP spider,scrapy).
- Passive Recon: Search Engine queries( google ), WHOIS(whois command-line tool), DNS(dig,nslookup,host,dnsenum,fierce,dnsrecon), Web archives analysis ,socaila media analysis and code repository
### WHOIS
It 's used to query and response protocol designed to access databases that stores information about registered internet resources'
```
 whois inlanefreight.com
```
### DNS Domain Name System

The DNS works by mapping domain names to actual Ip addresses that computer use to communicate

**How DNS works**

![[Pasted image 20260602120930.png]]
- Your computer asks for direction dns query
- The dns resolver check its map recusive lookup
- Root name server points the way
- TLD name server narrow it down
- Authoritative name server delivers the address
- The DNs resolver returns the information
- your computer connects
#### The Hosts File
it's a simple text file used to map hostnames to ip addresses manually and bypass domain name resolution process C:\Windows\System32\drivers\etc\hosts on windows and /etc/hosts on linux.

A zone is a distinct part of the domain namespace that a specific entity or admin manages eg mail.example.com or blog.example.com.
 


| Record Type | Full Name                 | Description                                                  |
| ----------- | ------------------------- | ------------------------------------------------------------ |
| A           | Address record            | ipv4                                                         |
| AAAA        | address record ipv6       | ipv6                                                         |
| CNAME       | Canonical Name record     | Create an alias for hostname                                 |
| MX          | Mail exchange record      | specifies the mail server                                    |
| NS          | Name server record        | delegates a dns wone to a specific authoritative name server |
| TXT         | Text record               | Stores arbitrary text information                            |
| SOA         | Start of authority record | specifies administrative infos about dns zone                |
| SRV         | service record            | defines the hostname and port number for specific services   |
| PTR         | pointer record            | used for reverse dns lookups etc                             |
|             |                           |                                                              |
### DIGGING DNS
DNS Tools
	- dig : Versatile DNS lookup tool that supports various query types (A, MX, NS, TXT, etc.) and detailed output.
	- nslookup : Simpler DNS lookup tool, primarily for A, AAAA, and MX records
	- host : Streamlined DNS lookup tool with concise output
	- dnsenum : Automated DNS enumeration tool, dictionary attacks, brute-forcing, zone transfers (if allowed)
	- fierce : DNS reconnaissance and subdomain enumeration tool with recursive search and wildcard detection
	- dnsrecon : Combines multiple DNS reconnaissance techniques and supports various output formats
	- Theharvester : OSINT tool that gathers information from various sources, including DNS records (email addresses).
	- online dns lookup serivces
Domain information Groper (dig)

|`dig domain.com`|Performs a default A record lookup for the domain.|
|`dig domain.com A`|Retrieves the IPv4 address (A record) associated with the domain.|
|`dig domain.com AAAA`|Retrieves the IPv6 address (AAAA record) associated with the domain.|
|`dig domain.com MX`|Finds the mail servers (MX records) responsible for the domain.|
|`dig domain.com NS`|Identifies the authoritative name servers for the domain.|
|`dig domain.com TXT`|Retrieves any TXT records associated with the domain.|
|`dig domain.com CNAME`|Retrieves the canonical name (CNAME) record for the domain.|
|`dig domain.com SOA`|Retrieves the start of authority (SOA) record for the domain.|
|`dig @1.1.1.1 domain.com`|Specifies a specific name server to query; in this case 1.1.1.1|
|`dig +trace domain.com`|Shows the full path of DNS resolution.|
|`dig -x 192.168.1.1`|Performs a reverse lookup on the IP address 192.168.1.1 to find the associated host name. You may need to specify a name server.|
|`dig +short domain.com`|Provides a short, concise answer to the query.|
|`dig +noall +answer domain.com`|Displays only the answer section of the query output.|
|`dig domain.com ANY`|Retrieves all available DNS records for the domain (Note: Many DNS servers ignore `ANY` queries to reduce load and prevent abuse, as per [RFC 8482](https://datatracker.ietf.org/doc/html/rfc8482)).|

```
dig +short hackthebox.com : just show the answer withour any further informartion
```

### Subdomains

They are extensions of the main domain oftern created to organise and seperate different sections or functionalities of a website
Subdomain enumeration is the process of systematically identifying and listing these subdomains. they are typically represented by A(or AAAA) whcih map the subdomain name ot ist corresponding Ip address .
A more common subdomain enum technique is brute force enum where is a list of word is usde to test against potential subdomain in the domain name . tools like dnsenum , ffuf and gobuster can automate this proecess.
The process beaks down to 4 steps:
- wordlist selection
- Iteration and querying 
- DNS lookup
- Filtering and validation
```
dnsenum --enum inlanefreight.com -f /usr/share/seclists/Discovery/DNS/subdomains-top1million-20000.txt -r : -r enables recursive subdomain brute forcing d
```
### DNS Zone Transfers
A DNS wone transfer is essentially a wholesale copy of all DNS records within a zone from one server to another
![[Pasted image 20260602131038.png]]

- Zone transfer Request (AXFR)
- SOA Record transfer
- DNS Record Transmission
- Zone transfer complete
- Acknowledgement
```
dig axfr @nswtm1.dig1.ninja zonetransfer.me : command to request a zone transfer
```
### VHosts
```
gobuster vhost -u http://<target_IP_address> -w <wordlist_file> --append-domain
```

## Fingerprinting
### Fingerprinting Techniques
- Banner grabbing
- Analysing http headers
- probing for spacific responses
- Analysing page content
Fingerprinting inlanefreight.com
```
 curl -i inlanefreight.com : banner grabbing we can see the version of os running
 curl -I https://www.inlanefreight.com : verify redirects and file\folder redirected
 wafw00f inlanefreight.com : usde to determine if a website is prtected by a wordfence web applicatioin firewall
 nikto -h inlanefreight.com -Tuning b : identifies outdated software,insecure files or config and other potential security risks
 
```

### Crawling
It is the automated process of systematically browsing the world wide web. It follows link from one page to another collecting informations
![[Pasted image 20260602142506.png]]
`Breadth-first crawling` prioritizes exploring a website's width before going deep. It starts by crawling all the links on the seed page, then moves on to the links on those pages, and so on. This is useful for getting a broad overview of a website's structure and content.

![[Pasted image 20260602142528.png]]
t, `depth-first crawling` prioritizes depth over breadth. It follows a single path of links as far as possible before backtracking and exploring other paths. This can be useful for finding specific content or reaching deep into a website's structure.
